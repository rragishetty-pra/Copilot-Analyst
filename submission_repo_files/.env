# Anthropic API key (used by the answer-generation layer, not the ingestion pipeline itself)
ANTHROPIC_API_KEY=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

# Local encryption key for documents at rest.
# Generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Keep this OUT of version control. If it's lost, encrypted files in data/raw cannot be recovered.
DOC_ENCRYPTION_KEY=

# Google Drive folder that documents get uploaded into (defaults to the
# "Copilot Analyst" folder already in config.py — override only if you move
# the corpus to a different folder).
DRIVE_FOLDER_ID=
